## 1. 코딩 에이전트에게 맡긴 부분
- Express 서버의 외부 API 호출에 대한 try-catch 및 오류 응답 처리를 개선했습니다.
- Express에서 반환하는 JSON 데이터를 React에서 렌더링 하는 구조를 구현했습니다.
- React 개발 서버에서 백엔드 API를 호출할 수 있도록 proxy 설정을 추가했습니다.
- 기존 방송 목록 API에 GNB API의 카테고리 데이터를 병합했습니다.
- 방송 목록을 테이블 형태로 표시하도록 React UI를 수정했습니다.

## 2. 직접 판단하거나 직접 구현한 부분
- 라방과 홈쇼핑 API의 응답 구조를 확인하고 공통 필드로 통합하는 방식을 결정했습니다.
- 방송 시간 데이터의 형식 차이를 확인하고 유형별 시간 포맷을 구현했습니다.
- 과제 요구사항에 맞춰 각 유형별 최대 10개 방송을 표시하도록 구현했습니다.
- 방송 상태값을 확인하여 라이브중, 정상종료, 지연/취소, 기록없음으로 표시하도록 구현했습니다.

## 3. 에이전트 결과를 고친 지점 (한 곳 이상, 구체적으로)
- 방송 시간 데이터의 실제 형식이 라방과 홈쇼핑에서 다른 것을 확인한 후 시간 포맷팅 로직을 수정했습니다.

## 4. 아직 완전히 이해하지 못한 부분 (있으면)
- JavaScript와 React, Node.js를 깊게 학습한 상태가 아니어서 이번 과제에서 사용한 React의 상태 관리와 useEffect 동작, Node.js/Express의 요청 처리 흐름을 완전히 숙지하지는 못했습니다.
- 외부 API -> Express -> React -> 화면으로 이어지는 전체 데이터 흐름은 구현 과정에서 확인했지만, 각 코드가 동작하는 원리까지는 추가 학습이 필요합니다.
- 제출 후 이번 과제의 코드를 처음부터 다시 살펴보며 각 부분의 동작 원리와 데이터 흐름을 학습할 예정입니다.